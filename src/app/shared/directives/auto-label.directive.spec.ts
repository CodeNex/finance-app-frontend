import { AutoLabelDirective } from './auto-label.directive';

describe('AutoLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoLabelDirective();
    expect(directive).toBeTruthy();
  });
});
